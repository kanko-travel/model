steps:
    - label: ":docker: Build"
      plugins:
          - docker-compose#v5.2.0:
                build: app
                push: app

    - wait
    - label: "Run tests"
      command: ./test.sh
      plugins:
          - docker-compose#v5.2.0:
                run: app
